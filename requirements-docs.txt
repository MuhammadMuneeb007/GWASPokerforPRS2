# Pinned below 2.0 deliberately. MkDocs 2.0 removes the plugin system and the
# theming system with no migration path, which would break this site's build.
mkdocs>=1.6,<2.0
mkdocs-material>=9.5,<10.0
pymdown-extensions>=10.7
