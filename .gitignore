# Python
__pycache__/
*.py[cod]
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.installed.cfg

# Environments
.venv/
venv/
env/
.conda/

# Testing / tooling
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.ruff_cache/
.mypy_cache/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# GWASPoker outputs written into the working directory
/search_results.csv
/search_results.json
/probe_results.json
/prs_assessment.csv
/prs_assessment.json
/gwaspoker_report.html
/provenance*.json
/gwaspoker_failures.jsonl
/failures.jsonl
/benchmark_results*.csv
/benchmark_results*.json
/metrics*.json
/sweep.json
/manifest_filled*.csv
/filled_*.csv

# Downloaded summary statistics (never commit multi-GB data)
/downloads/
*_extracted/
*.tsv.gz
*.txt.gz
*.csv.gz
*.vcf.gz
*.bgz
*.tar.gz
*.zip
*-meta.yaml
md5sum.txt
*.part

# ...but the test fixtures and the worked example outputs ARE part of the
# repository. They are small, deterministic, and regenerated by
# tests/fixtures/_generate.py.
!tests/fixtures/**
!examples/output/**

# Hugging Face model cache, if redirected into the repo
.hf_cache/
site/

# `extract` unpacks an archive member into a sibling `<name>_extracted/`
# directory beside the input; those are working files, never sources.
*_extracted/
